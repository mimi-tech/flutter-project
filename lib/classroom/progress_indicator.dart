import 'package:flutter/material.dart';

class ProgressIndicatorState extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: CircularProgressIndicator());
  }
}
